import { Meta } from '@storybook/addon-docs'

<Meta title="Introduction"/>

<div className="flex flex-col gap-3">
<span className="font-extrabold !text-5xl text-brand-700">Omega UI React Components</span>

<span className="!text-xl font-bold text-gray-200">v1.0.0</span>
</div>


***


<span className="font-bold !text-2xl">Overview</span>

Omega UI React Components is a set of UI components that are used in all the ecosystem of ACFaz

The library is built with React, TailiwndCSS and Radix UI


Have fun using it! 🚀


<span className="font-bold !text-2xl">Installation</span>

```bash
npm install @omega-ui/react
```

or

```bash
bun add @omega-ui/react
```




<span className="font-bold !text-2xl">Usage</span>

```jsx
import { Button } from '@omegacode-ui/react'

const App = () => {
  return (
    <Button>Click me</Button>
  )
}
```